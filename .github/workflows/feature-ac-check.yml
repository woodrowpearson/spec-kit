name: Feature Acceptance Criteria Check
on:
  pull_request:
    paths:
      - 'docs/**.md'
      - '**/*.swift'
jobs:
  ac:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.x'
      - run: pip install pyyaml
      - run: python3 scripts/lint-specs.py
